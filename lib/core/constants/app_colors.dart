import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const bg = Color(0xFF050A14);
  static const surface = Color(0xFF0D1626);
  static const surfaceElevated = Color(0xFF162035);
  static const border = Color(0xFF1E2D45);
  static const accent = Color(0xFF00D4FF);
  static const success = Color(0xFF30D158);
  static const warning = Color(0xFFFF6B35);
  static const danger = Color(0xFFFF2D55);
  static const textSec = Color(0xFF8899AA);
  static const textDim = Color(0xFF445566);
}
